$('main').appe;

$(() => {
	// CHALLENGE 1
	// loop 64 times and create 64 divs with alternating classes
	// for (let i = 0; i < 64; i++) {
	// 	let div = $('<div></div>');
	// 	if (i % 2 === 0) {
	// 		div = $(div).addClass('black');
	// 	} else {
	// 		div = $(div).addClass('red');
	// 	}
	// 	// append each div to the main
	// 	console.log(div);
	// 	$('main').append(div);
	// }
	// CHALLENGE 2: Checkerboard
	// let first = 'red';
	// let second = 'black';
	// for (let i = 0; i < 64; i++) {
	// 	let div = $('<div></div>');
	// 	if (i % 8 === 0) {
	// 		first = first === 'black' ? 'red' : 'black';
	// 		second = first === 'red' ? 'black' : 'red';
	// 	}
	// 	if (i % 2 === 0) {
	// 		div = $(div).addClass(first);
	// 	} else {
	// 		div = $(div).addClass(second);
	// 	}
	// 	// append each div to the main
	// 	$('main').append(div);
	// }
});
