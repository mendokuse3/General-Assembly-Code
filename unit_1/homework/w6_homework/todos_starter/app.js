$( () => {

    const items = []; //array of input items may not be necessary

    //code to take items from input box and add to array;

    // then take the item input and create a div class to-do-items, h2 of "input" and button (completed)

    //on click of completed button append current div to completed item list change button id to remove

    //on click of remove button remove div


});